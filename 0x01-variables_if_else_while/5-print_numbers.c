#include <stdio.h>
/**
 *  * main - Program entry point
 *    * Return: 0 on success. Error code otherwise
 *     **/
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}
	printf("\n");
	return (0);
}
